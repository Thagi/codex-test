# Common configuration
BACKEND_HOST=backend
BACKEND_PORT=8000
FRONTEND_PORT=8501
# Use the direct bolt scheme so the driver skips routing table discovery.
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j_dev_password
OLLAMA_BASE_URL=http://host.containers.internal:11434
OLLAMA_MODEL=gpt-oss-20b
# Optional guardrail for GPT-to-GPT jobs; leave unset to disable the timeout
# SIMULATION_TIMEOUT_SECONDS=300
# Limit the number of tokens each simulated agent and summary may generate (0 disables the cap)
# SIMULATION_MAX_TOKENS_PER_MESSAGE=512
# Fail fast when the Ollama endpoint does not respond within the allotted seconds (0 disables the guard)
# SIMULATION_GENERATION_TIMEOUT_SECONDS=60
CORS_ORIGINS=*
